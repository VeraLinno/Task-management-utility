services:
  public-gateway:
    image: nginx:1.27-alpine
    container_name: task-manager-gateway
    restart: unless-stopped
    ports:
      - "${GATEWAY_PORT:-85}:80"
    volumes:
      - ./nginx.vps.conf:/etc/nginx/conf.d/default.conf:ro
